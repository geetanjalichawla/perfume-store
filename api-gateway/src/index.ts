const greet = (name: string): string => {
    return `Hello, ${name}!`;
};

console.log(greet("World 23432"));
console.log("==================================================");
console.log(greet("World"));

export default greet;